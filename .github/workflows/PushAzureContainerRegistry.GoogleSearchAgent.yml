name: Build Linux Container Image and Push to ACR

on:
  push:
    paths:
      - GoogleSearchAgent/**
    branches:
      - main

jobs:
  build_and_push:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
        working-directory: GoogleSearchAgent
    steps:
      - uses: actions/checkout@v2

      - uses: azure/docker-login@v1
        with:
          login-server: dversegooglesearchagent.azurecr.io
          username: ${{ secrets.REGISTRY_USERNAME }}
          password: ${{ secrets.REGISTRY_PASSWORD }}

      - name: Build and Push Docker image
        run: |
          docker build -t dversegooglesearchagent.azurecr.io/myapp:${{ github.sha }} .
          docker push dversegooglesearchagent.azurecr.io/myapp:${{ github.sha }}
          
#  deploy:
#    needs: build_and_push
#    runs-on: ubuntu-latest
#    steps:
#      - name: Login to Azure
#        uses: azure/login@v1
#        with:
#          creds: ${{ secrets.AZURE_CREDENTIALS }}
#
#      - name: Deploy to Azure App Service
#        uses: azure/webapps-deploy@v2
#        with:
#          app-name: your-app-service-name # Replace with your app service name
#          slot-name: production # Optional: if using deployment slots
#          publish-profile: ${{ secrets.GOOGLE_SEARCH_AGENT_AZURE_PUBLISH_PROFILE }} # Alternative authentication method
#          images: 'dversegooglesearchagent.azurecr.io/myapp:${{ github.sha }}'

# This can be added later, Fontys has disabled the way to get access to my azure credentials. I cant get to my tenant_id
